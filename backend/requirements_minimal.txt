# Minimal requirements for backend to start
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.8.1
httpx[http2]==0.27.2
psutil==6.1.1
requests==2.32.3

# Database drivers (lightweight)
psycopg2-binary==2.9.10
redis==5.2.1

# Required for metrics (main.py imports app.core.metrics)
prometheus-client==0.21.1

# Authentication dependencies (CRITICAL for security)
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
email-validator==2.2.0

# Database ORM (required for auth system)
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.29.0

# Basic utilities
python-multipart==0.0.18
python-dotenv==1.0.1
uvloop==0.19.0