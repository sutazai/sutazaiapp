# SutazAI AI Agent Orchestrator - Production Requirements
# Inherits from base requirements

# Include base requirements
-r ../../requirements-base.txt

# Agent-specific overrides (if needed)
# Note: Using numpy<2.0 for compatibility with some agent libraries
# This will override the base requirement
numpy>=1.23.5,<2.0                  # Numerical computing - compatible version for agents