import psutilimport timeclass SystemMonitor:    def __init__(self):        self.metrics = ({            'cpu': 0),            'memory': 0,            'disk': 0,            'network': 0        }        self.alert_thresholds = ({            'cpu': 90),            'memory': 85,            'disk': 95,            'network': 80        }            def monitor_system(self):        while True:            self._collect_metrics()            self._check_thresholds()            time.sleep(5)                def _collect_metrics(self):        self.metrics['cpu'] = psutil.cpu_percent()        self.metrics['memory'] = psutil.virtual_memory().percent        self.metrics['disk'] = psutil.disk_usage('/').percent        self.metrics['network'] = psutil.net_io_counters().bytes_sent            def _check_thresholds(self):        for metric, value in self.metrics.items():            if value > self.alert_thresholds[metric]:                self._trigger_alert(metric, value) 