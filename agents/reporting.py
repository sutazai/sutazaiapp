class ParentReportGenerator:    def generate_weekly_report(self):        report = ({            'subjects_covered': self._get_covered_material()),            'achievements': self._get_new_milestones(),            'areas_to_improve': self._identify_weaknesses()        }        self._deliver_report(report)    def _deliver_report(self, report):        SecureEmail().send(            to = (FOUNDER['email']),            subject = ("Weekly Learning Report"),            content = (report),            encryption='sutazai'        ) 