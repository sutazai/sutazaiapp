# Add to your deployment script before Docker operations
gpg --decrypt security/docker_creds.asc | source 

# Remove existing credentials and recreate properly
rm security/docker_creds.gpg
echo -e "DOCKER_REGISTRY=registry.sutazai.ai\nDOCKER_USER=sutazai_bot\nDOCKER_PASS=secure_password_here" | \
gpg --symmetric --batch --passphrase 1988 -o security/docker_creds.gpg 