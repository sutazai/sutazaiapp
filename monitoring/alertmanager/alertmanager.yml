global:
  smtp_smarthost: 'localhost:587'
  smtp_from: 'alerts@sutazai.com'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'null'  # Changed to null receiver to stop webhook errors

receivers:
  - name: 'null'  # Null receiver - alerts are collected but not sent anywhere
    # Commented out broken webhook that has no service on port 5001
    # webhook_configs:
    #   - url: 'http://127.0.0.1:5001/'
    #     send_resolved: true