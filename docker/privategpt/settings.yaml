server:
  env_name: local

local:
  embedding:
    mode: local
  llm:
    mode: ollama
    max_new_tokens: 512
    context_window: 3900
    tokenizer: mistralai/Mistral-7B-Instruct-v0.2
  vectorstore:
    database: qdrant
EOF < /dev/null
