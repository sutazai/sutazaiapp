# SutazAI Alpine-Optimized   Requirements
# Stripped down dependencies optimized for edge computing
# Target: 50% dependency reduction from base-requirements- .txt
# Date: August 10, 2025

# Core web framework (single choice for size optimization)
fastapi>=0.104.1,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
pydantic>=2.9.0,<3.0.0

# HTTP client (compatible version)
httpx>=0.27.0,<0.28.0

# Essential utilities only
python-dotenv==1.0.0
pyyaml==6.0.1

# Monitoring (essential for edge deployment)
prometheus-client==0.19.0

# Database connectivity (core only)
redis==5.0.1
asyncpg==0.29.0  # Lighter PostgreSQL driver than SQLAlchemy

# AI/ML integration ( )
ollama==0.4.4

# System monitoring (essential for resource optimization)
psutil==5.9.6

# Async utilities
aiofiles==23.2.1