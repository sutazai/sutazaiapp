[model]
kind = "ollama"
model_id = "gpt-oss"
api_endpoint = "http://ollama:11434"

[llm]
provider = "ollama"
base_url = "http://ollama:11434"
model = "gpt-oss"

[embeddings]
provider = "ollama"
model = "nomic-embed-text"
