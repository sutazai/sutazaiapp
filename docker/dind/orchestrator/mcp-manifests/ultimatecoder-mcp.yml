apiVersion: mcp/v1
kind: MCPContainer
metadata:
  name: mcp-ultimatecoder
  labels:
    mcp.service: ultimatecoder
    mcp.tier: ai
spec:
  image: python:3.11-slim
  container_name: mcp-ultimatecoder
  command:
  - python
  - -m
  - ultimatecoder.server
  - --port
  - '3011'
  environment:
    MCP_PORT: '3011'
    MESH_ENABLED: 'true'
    SERVICE_NAME: ultimatecoder
    PYTHONUNBUFFERED: '1'
  ports:
    '13011': '3011'
  volumes:
    mcp_shared_data: /mcp-shared
    /opt/sutazaiapp: /opt/sutazaiapp:ro
  healthcheck:
    test:
    - CMD
    - wget
    - -q
    - --spider
    - http://localhost:3011/health
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 45s
  restart_policy: unless-stopped
  resources:
    limits:
      cpus: '1.0'
      memory: 1024M
    reservations:
      cpus: '0.2'
      memory: 128M
  networks:
  - sutazai-dind-internal
  labels:
    mcp.managed: 'true'
    mcp.protocol: http
    mcp.integration: ultimatecoder
    mcp.service: 'true'
