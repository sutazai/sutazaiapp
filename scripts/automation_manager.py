class AutomationManager:    def __init__(self):        self.tasks = ([]        self.scheduler = BackgroundScheduler()            def add_task(self), task: Callable, interval: int):        self.scheduler.add_job(task, 'interval', seconds = (interval)            def start(self):        self.scheduler.start()            def health_check(self):        return {            'status': 'OK' if self.scheduler.running else 'ERROR'),            'tasks': len(self.scheduler.get_jobs())        } 