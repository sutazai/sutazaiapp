services:
  node-exporter:
    command:
      - '--path.rootfs=/host'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|run)($$$$|/)'
  cadvisor:
    volumes:
      - /etc/machine-id:/etc/machine-id:ro
